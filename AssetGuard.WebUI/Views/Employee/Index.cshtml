@using AssetGuard.Entity
@model AssetGuard.WebUI.Models.EmployeeListViewModel

@{
    ViewData["Title"] = "Personel Listesi";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Aktif Personel Kayıtları</h3>
        <div class="card-tools">
            <a href="/Employee/Create" class="btn btn-primary btn-sm">
                <i class="fas fa-user-plus"></i> Yeni Personel Ekle
            </a>
        </div>
    </div>

    <div class="card-body p-0">
        <table class="table table-striped projects table-hover">
            <thead>
                <tr>
                    <th style="width: 5%">ID</th>
                    <th style="width: 25%">Ad Soyad</th>
                    <th style="width: 20%">Departman</th>
                    <th style="width: 15%">Durum</th>
                    <th style="width: 20%">İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Employees)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>
                            <strong>@item.FirstName @item.LastName</strong>
                        </td>
                        <td>
                            <span class="badge badge-secondary">@item.Department?.Name</span>
                        </td>
                        <td>
                            <span class="badge badge-success">Aktif</span>
                        </td>
                        <td class="project-actions text-right">
                            <a class="btn btn-info btn-sm" href="/Employee/Edit/@item.Id">
                                <i class="fas fa-pencil-alt"></i> Düzenle
                            </a>
                            <a class="btn btn-danger btn-sm" href="/Employee/Delete/@item.Id"
                               onclick="return confirm('@item.FirstName @item.LastName isimli personeli silmek istediğinize emin misiniz?');">
                                <i class="fas fa-trash"></i> Sil
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<!-- ================================================= -->
<!-- PERSONEL HATA MODALI (Zimmetleri Gösteren Kısım) -->
<!-- ================================================= -->
@if (Model.ErrorInfo != null)
{
    <div class="modal fade" id="deleteErrorModal" tabindex="-1" role="dialog" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content bg-danger">
                <div class="modal-header">
                    <h4 class="modal-title">
                        <i class="fas fa-exclamation-triangle mr-2"></i> @Model.ErrorInfo.EntityName Silinemedi!
                    </h4>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body" style="background-color: white; color: black;">
                    <p class="lead text-danger font-weight-bold">@Model.ErrorInfo.ErrorMessage</p>
                    <p>Bu personel silinemez! Çünkü üzerinde kayıtlı olan (aktif veya geçmiş) zimmet hareketleri bulunmaktadır:</p>

                    <div class="table-responsive" style="max-height: 300px; overflow-y: auto;">
                        <table class="table table-sm table-bordered table-striped">
                            <thead class="thead-light">
                                <tr>
                                    <th>Zimmetli Demirbaş ve Tarih</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.ErrorInfo.RelatedItems)
                                {
                                    <tr>
                                        <td><i class="fas fa-handshake text-muted mr-2"></i> @item</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="modal-footer justify-content-between" style="background-color: #f8f9fa; color: black;">
                    <button type="button" class="btn btn-outline-danger" data-dismiss="modal">Kapat</button>
                    <a href="/Assignment/Index" class="btn btn-danger">Zimmet Geçmişine Git</a>
                </div>
            </div>
        </div>
    </div>
}

@section Scripts {
    <script>
        $(document).ready(function () {
            // Controller'dan hata bilgisi geldiyse modalı patlat
            @if (Model.ErrorInfo != null)
            {
                    <text>
                        $('#deleteErrorModal').modal('show');
                    </text>
            }
        });
    </script>
}