<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>AssetGuard | Demirbaş Yönetim Sistemi</title>

    <!-- Google Font: Source Sans Pro -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,400i,700&display=fallback">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="~/plugins/fontawesome-free/css/all.min.css">
    <!-- Ionicons -->
    <link rel="stylesheet" href="https://code.ionicframework.com/ionicons/2.0.1/css/ionicons.min.css">
    <!-- Tempusdominus Bootstrap 4 -->
    <link rel="stylesheet" href="~/plugins/tempusdominus-bootstrap-4/css/tempusdominus-bootstrap-4.min.css">
    <!-- iCheck -->
    <link rel="stylesheet" href="~/plugins/icheck-bootstrap/icheck-bootstrap.min.css">
    <!-- Theme style -->
    <link rel="stylesheet" href="~/dist/css/adminlte.min.css">
    <!-- overlayScrollbars -->
    <link rel="stylesheet" href="~/plugins/overlayScrollbars/css/OverlayScrollbars.min.css">
</head>
<body class="hold-transition sidebar-mini layout-fixed">
    <div class="wrapper">

        <!-- 1. ÜST MENÜ (NAVBAR) -->
        <partial name="_Navbar" />

        <!-- 2. SOL MENÜ (SIDEBAR) -->
        <partial name="_Sidebar" />

        <!-- 3. DEĞİŞEN İÇERİK ALANI (CONTENT WRAPPER) -->
        <div class="content-wrapper">
            <!-- Sayfa Başlığı (Breadcrumb) -->
            <div class="content-header">
                <div class="container-fluid">
                    <div class="row mb-2">
                        <div class="col-sm-6">
                            <!-- Her sayfa buraya kendi başlığını gönderecek -->
                            <h1 class="m-0">@ViewData["Title"]</h1>
                        </div>
                    </div>
                </div>
            </div>
            <!-- /.content-header -->
            <!-- Ana İçerik -->
            <section class="content">
                <div class="container-fluid">
                    @RenderBody()
                </div>
            </section>
        </div>

        <!-- 4. ALT BİLGİ (FOOTER) -->
        <footer class="main-footer">
            <strong>Copyright &copy; @DateTime.Now.Year <a href="#">AssetGuard</a>.</strong>
            Tüm hakları saklıdır.
            <div class="float-right d-none d-sm-inline-block">
                <b>Version</b> 1.0.0 (Alpha)
            </div>
        </footer>

        <!-- Control Sidebar -->
        <aside class="control-sidebar control-sidebar-dark">
            <!-- Control sidebar content goes here -->
        </aside>
    </div>
    <!-- ./wrapper -->
    <!-- jQuery -->
    <script src="~/plugins/jquery/jquery.min.js"></script>
    <!-- jQuery UI 1.11.4 -->
    <script src="~/plugins/jquery-ui/jquery-ui.min.js"></script>
    <!-- Resolve conflict in jQuery UI tooltip with Bootstrap tooltip -->
    <script>
        $.widget.bridge('uibutton', $.ui.button)
    </script>
    <!-- Bootstrap 4 -->
    <script src="~/plugins/bootstrap/js/bootstrap.bundle.min.js"></script>
    <!-- ChartJS -->
    <script src="~/plugins/chart.js/Chart.min.js"></script>
    <!-- Sparkline -->
    <script src="~/plugins/sparklines/sparkline.js"></script>
    <!-- jQuery Knob Chart -->
    <script src="~/plugins/jquery-knob/jquery.knob.min.js"></script>
    <!-- daterangepicker -->
    <script src="~/plugins/moment/moment.min.js"></script>
    <script src="~/plugins/daterangepicker/daterangepicker.js"></script>
    <!-- Tempusdominus Bootstrap 4 -->
    <script src="~/plugins/tempusdominus-bootstrap-4/js/tempusdominus-bootstrap-4.min.js"></script>
    <!-- Summernote -->
    <script src="~/plugins/summernote/summernote-bs4.min.js"></script>
    <!-- overlayScrollbars -->
    <script src="~/plugins/overlayScrollbars/js/jquery.overlayScrollbars.min.js"></script>
    <!-- AdminLTE App -->
    <script src="~/dist/js/adminlte.js"></script>

    <!-- Sayfaya özel scriptler buraya gelecek -->
    @await RenderSectionAsync("Scripts", required: false)


    <script>
        $(document).ready(function () {
            var savedColor = localStorage.getItem('assetGuard_sidebarColor');
            var savedDarkMode = localStorage.getItem('assetGuard_darkMode');

            var sidebar = $('.main-sidebar');
            var brandLink = $('.brand-link');
            var body = $('body');
            var header = $('.main-header');

            // 1. Dark Mode
            if (savedDarkMode === 'true') {
                body.addClass('dark-mode');
                header.removeClass('navbar-white navbar-light').addClass('navbar-dark');
            }

            // 2. Sidebar Tam Arka Plan Rengi
            if (savedColor && savedColor !== 'default') {
                // Önce temizle
                var colors = ['bg-primary', 'bg-warning', 'bg-danger', 'bg-success', 'bg-indigo', 'bg-navy', 'bg-purple', 'bg-fuchsia', 'bg-pink', 'bg-orange', 'bg-lime', 'bg-teal', 'bg-white', 'bg-light', 'bg-dark'];
                sidebar.removeClass(colors.join(' '));
                brandLink.removeClass(colors.join(' '));

                // Sonra boya
                sidebar.addClass('bg-' + savedColor);
                brandLink.addClass('bg-' + savedColor);
            } else {
                // Varsayılan logo rengi
                brandLink.addClass('bg-primary');
            }
        });
    </script>
</body>



</html>