@model AssetGuard.WebUI.Models.CategoryListViewModel

@{
    ViewData["Title"] = "Kategori Yönetimi";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Tüm Kategoriler</h3>
        <div class="card-tools">
            <a href="/Category/Create" class="btn btn-primary btn-sm">
                <i class="fas fa-plus"></i> Yeni Kategori Ekle
            </a>
        </div>
    </div>

    <div class="card-body p-0">
        <table class="table table-striped projects table-hover">
            <thead>
                <tr>
                    <th style="width: 10%">ID</th>
                    <th style="width: 70%">Kategori Adı</th>
                    <th style="width: 20%">İşlemler</th>
                </tr>
            </thead>
            <tbody>
                <!-- ViewModel içindeki Categories listesini dönüyoruz -->
                @foreach (var item in Model.Categories)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>
                            <a>@item.Name</a>
                        </td>
                        <td class="project-actions text-right">
                            <a class="btn btn-info btn-sm" href="/Category/Edit/@item.Id">
                                <i class="fas fa-pencil-alt"></i> Düzenle
                            </a>
                            <a class="btn btn-danger btn-sm" href="/Category/Delete/@item.Id" onclick="return confirm('Bu kategoriyi silmek istediğinize emin misiniz?');">
                                <i class="fas fa-trash"></i> Sil
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<!-- ================================================= -->
<!-- HATA MODALI (Sadece ErrorInfo doluysa oluşur) -->
<!-- ================================================= -->
@if (Model.ErrorInfo != null)
{
    <div class="modal fade" id="deleteErrorModal" tabindex="-1" role="dialog" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content bg-danger">

                <div class="modal-header">
                    <h4 class="modal-title">
                        <i class="fas fa-exclamation-triangle mr-2"></i> @Model.ErrorInfo.EntityName Silinemedi!
                    </h4>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>

                <div class="modal-body" style="background-color: white; color: black;">
                    <p class="lead text-danger font-weight-bold">@Model.ErrorInfo.ErrorMessage</p>
                    <p>Silme işlemini yapabilmek için önce aşağıdaki ürünlerin kategorisini değiştirmeli veya silmelisiniz:</p>

                    <div class="table-responsive" style="max-height: 300px; overflow-y: auto;">
                        <table class="table table-sm table-bordered table-striped">
                            <thead class="thead-light">
                                <tr>
                                    <th>Engelleyen Kayıtlar</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.ErrorInfo.RelatedItems)
                                {
                                    <tr>
                                        <td><i class="fas fa-link text-muted mr-2"></i> @item</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>

                <div class="modal-footer justify-content-between" style="background-color: #f8f9fa; color: black;">
                    <button type="button" class="btn btn-outline-danger" data-dismiss="modal">Kapat</button>
                    <!-- Modal kapandığında kullanıcıyı temiz bir Index sayfasına yönlendir -->
                    <a href="/Category/Index" class="btn btn-danger">Tamam</a>
                </div>
            </div>
        </div>
    </div>
}

@section Scripts {
    <script>
        $(document).ready(function () {
            // Eğer ErrorInfo doluysa Modalı otomatik aç
            @if (Model.ErrorInfo != null)
            {
                    <text>
                        $('#deleteErrorModal').modal('show');
                    </text>
            }
        });
    </script>
}